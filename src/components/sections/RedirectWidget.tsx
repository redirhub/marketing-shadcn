"use client";

// Stub RedirectWidget — matches the interface from the old Chakra version.
// Replace with the full implementation when ready.
export default function RedirectWidget({ fixed }: { fixed?: string }) {
  return (
    <div className="w-full max-w-6xl mx-auto p-6 bg-white/95 backdrop-blur-md rounded-2xl shadow-lg border border-white/20">
      <p className="text-center text-sm text-gray-400">RedirectWidget placeholder</p>
    </div>
  );
}
